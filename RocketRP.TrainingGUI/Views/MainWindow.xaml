<Window x:Class="RocketRP.TrainingGUI.Views.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="RocketRP - Training Pack" Width="700" Height="380">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<Button Grid.Row="0" Grid.Column="0" Content="Importer un .Tem / .json..." Padding="10,4" Command="{Binding ImportCommand}" />
		<TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,0,0,0" VerticalAlignment="Center"
				   TextTrimming="CharacterEllipsis" Text="{Binding JsonPath, TargetNullValue=Aucun fichier chargé}" />

		<TextBlock Grid.Row="1" Grid.Column="0" Margin="0,14,0,0" VerticalAlignment="Center" Text="Nom du JSON" />
		<TextBox Grid.Row="1" Grid.Column="1" Margin="10,14,0,0" VerticalAlignment="Center" IsEnabled="{Binding HasJson}"
				 Text="{Binding JsonFileName, UpdateSourceTrigger=PropertyChanged}" />
		<Button Grid.Row="1" Grid.Column="2" Margin="10,14,0,0" Padding="10,4" Content="Renommer" Command="{Binding RenameCommand}" />

		<Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,14,0,0" Padding="10,6" HorizontalAlignment="Left"
				Content="Convertir le JSON en .Tem" Command="{Binding ConvertToTemCommand}" />

		<TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,14,0,0" IsReadOnly="True"
				 FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap"
				 Text="{Binding Log, Mode=OneWay}" />
	</Grid>
</Window>
